<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    /**
     * Seed the application's database.
     */
    public function run(): void
    {
        // Intentionally empty.
        // Run explicit seeders such as StaffHubBootstrapSeeder only when
        // the environment-specific credentials and device settings are prepared.
    }
}
